modules = ["python-3.12"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "cd present-ponder && npm install && npm run build && cd .. && python -m uvicorn app.asgi:app --host 0.0.0.0 --port 5000"]

[[ports]]
localPort = 5000
externalPort = 80
